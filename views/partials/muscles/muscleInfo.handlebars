<div id="dropChest" class="dropdown-content" data-state="hidden">
	<a href="#">{{this.name}}
		<p>{{this.description}}</p>
		<button data-name="{{this.name}}" data-description="{{this.description}}" class="saveWorkout">Save</button>
	</a>
</div>